export interface Song {
  id: string;
  author: string;
  title: string;
  song_url: string;
  thumbnail: string;
}

export interface Pack {
  id: string;
  author: string; // of the pack
  title: string;
  thumbnail: string;
}
